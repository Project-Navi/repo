# CLAUDE.md

This file provides guidance to Claude Code when working with code in this repository.

## Project Overview

**{{ spec.name }}**{% if spec.description is defined %} — {{ spec.description }}{% endif %}

- **Language:** {{ spec.language }}
- **Python version:** >= {{ spec.python_version }}
{% if spec.structure is defined %}- **Source:** `{{ spec.structure.src_dir }}/`
- **Tests:** `{{ spec.structure.test_dir | default("tests") }}/`
{% endif %}

## Development Commands

```bash
# Install dependencies
uv sync

# Run tests
uv run pytest {{ spec.structure.test_dir | default("tests") }}/ -v

# Run tests with coverage
uv run pytest {{ spec.structure.test_dir | default("tests") }}/ -v --cov={{ spec.structure.src_dir }} --cov-report=term-missing

# Lint
uv run ruff check {{ spec.structure.src_dir }}/ {{ spec.structure.test_dir | default("tests") }}/

# Format
uv run ruff format {{ spec.structure.src_dir }}/ {{ spec.structure.test_dir | default("tests") }}/

# Type check
uv run mypy {{ spec.structure.src_dir }}/

# Security scan
uv run bandit -r {{ spec.structure.src_dir }} -ll

# Run all quality checks
uv run ruff format --check {{ spec.structure.src_dir }}/ {{ spec.structure.test_dir | default("tests") }}/ && uv run ruff check {{ spec.structure.src_dir }}/ {{ spec.structure.test_dir | default("tests") }}/ && uv run mypy {{ spec.structure.src_dir }}/

# Pre-commit (run all hooks)
pre-commit run --all-files
```

## Code Quality Standards

- Line length: 100 characters
- Linter: ruff (select: E, F, I, N, W, UP, B, RUF, C4)
- Type checking: mypy (strict mode)
- Security: bandit
- Secret detection: detect-secrets with baseline

## Commit Conventions

- Use conventional commits: `feat:`, `fix:`, `chore:`, `docs:`, `test:`, `refactor:`
- Keep commits atomic — one logical change per commit
- Run quality checks before committing

## Scope Boundaries

- Do not modify files outside `{{ spec.structure.src_dir }}/` and `{{ spec.structure.test_dir | default("tests") }}/` without explicit approval
- Do not bump `requires-python` or change dependency version constraints without discussion
- Document pre-existing violations in DEBT.md rather than fixing them silently
